/**
 * Represents a class room class
 */
export default class ClassRoom {
/**
 * Creates a new class
 * @param {Number} maxStudentsSize - the maximum number of
 * students in a class. 
 */
  constructor(maxStudentsSize) {
    this._maxStudentsSize = maxStudentsSize;
  }
}